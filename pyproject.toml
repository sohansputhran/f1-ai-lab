[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "f1-ai-lab"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
# Auto-discover all packages (project1_race_predictor, project2_race_analyst, etc.)
where = ["."]
